# 2026-01-27 MCP model name lookup

## Notes
- Looked up OpenAI docs for GPT-5.2 model IDs and xhigh reasoning effort.
- Use `gpt-5.2` or `gpt-5.2-pro`; xhigh is a reasoning setting, not a separate model.

## Open Questions
- Which MCP client/server config is being used for the request?
